package junit.tutorial;

/**
 * JUnit実践入門の第1章で作成する計算機プログラム.
 * リスト1.7 floatに対応した除算メソット
 * @author shuji.w6e
 */
public class Calculator {

    public int multiply(int x, int y) {
        return x * y;
    }

    public float divide(int x, int y) {
        return (float) x / (float) y;
    }
}